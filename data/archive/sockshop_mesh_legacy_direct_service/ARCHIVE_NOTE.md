# Legacy Archive Note

This directory contains the old Istio mesh data collected with the legacy direct-service load and the old compressed batch outputs.

It is preserved only as historical reference and should not be used as the current mainline result set.

Current mainline data roots:

- `data/sockshop_mesh_business/`
- `data/sockshop_mesh_candidate/`
