kind: PodChaos
apiVersion: chaos-mesh.org/v1alpha1
metadata:
  namespace: sock-shop
  name: el-e5-pod-kill-orders
spec:
  selector:
    namespaces:
      - sock-shop
    labelSelectors:
      name: orders
  mode: one
  action: pod-kill
