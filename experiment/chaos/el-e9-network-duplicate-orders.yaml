kind: NetworkChaos
apiVersion: chaos-mesh.org/v1alpha1
metadata:
  namespace: sock-shop
  name: el-e9-network-duplicate-orders
spec:
  selector:
    namespaces:
      - sock-shop
    labelSelectors:
      name: orders
  mode: all
  action: duplicate
  duplicate:
    duplicate: "5"
    correlation: "0"
