kind: NetworkChaos
apiVersion: chaos-mesh.org/v1alpha1
metadata:
  namespace: sock-shop
  name: el-e15-network-duplicate-carts
spec:
  selector:
    namespaces:
      - sock-shop
    labelSelectors:
      name: carts
  mode: all
  action: duplicate
  duplicate:
    duplicate: "5"
    correlation: "0"
