kind: NetworkChaos
apiVersion: chaos-mesh.org/v1alpha1
metadata:
  namespace: sock-shop
  name: final-e2-network-delay-payment
spec:
  selector:
    namespaces:
      - sock-shop
    labelSelectors:
      name: payment
  mode: all
  action: delay
  delay:
    latency: "300ms"
    correlation: "0"
    jitter: "0ms"
