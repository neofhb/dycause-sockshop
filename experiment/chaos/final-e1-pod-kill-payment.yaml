kind: PodChaos
apiVersion: chaos-mesh.org/v1alpha1
metadata:
  namespace: sock-shop
  name: final-e1-pod-kill-payment
spec:
  selector:
    namespaces:
      - sock-shop
    labelSelectors:
      name: payment
  mode: one
  action: pod-kill
