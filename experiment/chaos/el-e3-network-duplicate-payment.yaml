kind: NetworkChaos
apiVersion: chaos-mesh.org/v1alpha1
metadata:
  namespace: sock-shop
  name: el-e3-network-duplicate-payment
spec:
  selector:
    namespaces:
      - sock-shop
    labelSelectors:
      name: payment
  mode: all
  action: duplicate
  duplicate:
    duplicate: "5"
    correlation: "0"
