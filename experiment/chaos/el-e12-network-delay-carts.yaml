kind: NetworkChaos
apiVersion: chaos-mesh.org/v1alpha1
metadata:
  namespace: sock-shop
  name: el-e12-network-delay-carts
spec:
  selector:
    namespaces:
      - sock-shop
    labelSelectors:
      name: carts
  mode: all
  action: delay
  delay:
    latency: "300ms"
    correlation: "0"
    jitter: "0ms"
