kind: NetworkChaos
apiVersion: chaos-mesh.org/v1alpha1
metadata:
  namespace: sock-shop
  name: el-e14-network-corrupt-carts
spec:
  selector:
    namespaces:
      - sock-shop
    labelSelectors:
      name: carts
  mode: all
  action: corrupt
  corrupt:
    corrupt: "2"
    correlation: "0"
